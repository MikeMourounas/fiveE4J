package com.fiveE4J.textgen.datasets;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/**
 * Don't really need this right now, will populate once scraper is better
 */
public class MMScraperTest {
    private static final Logger logger = LoggerFactory.getLogger(MMScraperTest.class);
}
